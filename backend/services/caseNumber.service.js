export { getNextSequence, generateCaseNumber } from '../controllers/case.controller.js';
import { getNextSequence, generateCaseNumber } from '../controllers/case.controller.js';

export default {
    generateCaseNumber,
    getNextSequence
};

